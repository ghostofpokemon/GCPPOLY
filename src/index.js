import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
import PC from './PC';

ReactDOM.render(
  <React.StrictMode>
    <PC />
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
